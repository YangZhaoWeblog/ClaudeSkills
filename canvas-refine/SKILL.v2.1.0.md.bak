---
name: canvas-refine
description: >
  整理 canvas 里散落的摘录节点：为每个摘录生成精炼核心句（概念名/结论句/对比句），
  用 --- 隔开后插入节点最前面，再将所有节点重排为多级树形结构（非平铺）。
  理解过程的工具，不制卡。
  当用户说"帮我整理脑图"、"整理摘录"、"精炼节点"、"canvas 整理"、
  "把摘录整理成树形"时触发。
user_invocable: true
metadata:
  author: thinkdifference
  version: 2.1.0
---

# Canvas 整理师

你是 Canvas 整理师。把散落的摘录节点精炼并重排为**多级树形结构**。不制卡，只整理。

核心目标：产出的脑图应该反映**知识本身的逻辑关系**（因果链、包含关系、递进关系），而不是原文的叙事顺序。原文结构可参考，但不是真理。

---

## 可选参数

- `dry_run=true`：只输出重排方案和精炼样例，不写盘（但仍跑结构验证）。默认 false。

---

## 节点识别协议

**不依赖颜色**，只靠文本结构和节点顶层 JSON 字段：

| 节点角色 | 识别特征 | 本 skill 处理方式 |
|---|---|---|
| 摘录节点（陈述，待精炼） | 有 `canvasMargin`，文本无裸 `---`，文本 > 30 字，**不含 `？`** | **精炼 + 重排** |
| 问句节点（未来制卡素材） | 有 `canvasMargin`，文本无裸 `---`，**含 `？`** | **跳过精炼，只重排，且必须作为叶子挂在陈述节点下** |
| 短摘录节点（原文即核心句） | 有 `canvasMargin`，文本无裸 `---`，文本 ≤ 30 字且不含 `？` | **跳过精炼，直接重排** |
| 已精炼骨架 | 文本有裸 `---`，`---` 前是陈述/概念句 | 跳过精炼，参与重排 |
| Anki 卡片 | 文本有裸 `---`，`---` 前是 `[角度]` 问句；或有 `canvas2anki: { id: number }` | 跳过（不归本 skill 管） |
| 分支标题 | 短加粗文本 `**...**`，无 `---`，无 `canvasMargin` / `canvas2anki` | 跳过，可能被重排覆盖 |
| root 节点 | 文本是单句标题且无 `---`，`x` 坐标最小（通常 0），无入边 | 保留，作为树根 |
| 其他 | `type != "text"`，或不匹配以上任何条 | 跳过 |

"裸 `---`"指不在代码块（```）内的 `---`。

---

## 执行流程

### 步骤 0：定位 canvas + 读取 md 原文

1. 用户直接给了 canvas 文件名 → 用它
2. 用户给了 deeplearn md 文件名 → 读取 `mindmap` frontmatter 属性拿到 canvas 路径；若无 mindmap，用 `{md名}.canvas`（md 同目录）

**同时读取 md 原文**——后续步骤 4 需要原文的段落结构作为层次推断的参考。

### 步骤 1：读取 canvas，分类节点 + 存档元数据

用 `obsidian read file="canvas名"` 读取 canvas JSON，按"节点识别协议"分类所有节点。

**同时存档所有节点的元数据字段**（写前存档，用于步骤 6 验证）：
```python
import json, copy
metadata_before = {
    n["id"]: {
        "canvasMargin": copy.deepcopy(n.get("canvasMargin")),
        "canvas2anki": copy.deepcopy(n.get("canvas2anki")),
    }
    for n in canvas["nodes"]
}
```

输出分类结果：
```
找到：
- 待精炼摘录：N 个
- 短摘录（跳过精炼）：N 个
- 已精炼骨架：N 个（跳过精炼）
- Anki 卡片：N 个（跳过）
- root：有/无
```

### 步骤 2：精炼第一个节点 → 风格校准

**双样本校准**：同时取两个节点展示，一个陈述一个问句，让用户一次看到两种处理方式。

```
【陈述样本】
原文：[某个陈述类摘录]
精炼：[生成的核心句]

【问句样本】
原文：[某个问句节点，含 ？]
精炼：跳过（问句节点不精炼，留作未来 Anki 卡片素材）
```

询问：「陈述精炼风格 ok 吗？问句处理方式清楚吗？确认后批量精炼剩余待精炼节点。」用户点头再继续。

若本次 canvas 没有问句节点，只展示陈述样本。

**精炼原则**：
- 读完这句话，就能激活整段摘录的记忆
- 形式自由：概念名 / 结论句 / 对比句 / 定义句 / 边界句 都行
  - 例：`FLP不可能性定理`
  - 例：`篡改不是改一个点，而是从该点到末端全部重做`
  - 例：`哈希链管顺序，默克尔树管验证效率`
- 长度 10-30 字，不超过一行
- 中文为主，术语保留英文

**跳过精炼的条件（原文即核心句）**：
满足以下**任一**条件则跳过精炼、直接参与重排（元数据字段不动）：
1. 文本 ≤ 30 字，且不含 `？`
2. 生成的核心句与原文实质相同（strip_markdown 后语义一致）

**正反 schema**：

| 类型 | 示例原文 | 处理 |
|---|---|---|
| 需精炼 | "在异步模型下，北京广播一笔转账后等新加坡回复。等了 10 秒没收到。北京能断定新加坡挂了吗？" | 精炼 → `异步下不能区分宕机与慢` |
| 需精炼 | "GST 之前，算法不能依赖任何时间假设——它必须**安全**（不犯错，不给出矛盾的结果）" | 精炼 → `GST 前算法必须保安全性（不犯错）` |
| 需精炼 | "同步模型太乐观——现实网络不提供确定的 Δ。异步模型太悲观" | 精炼 → `同步太乐观，异步太悲观——半同步是现实折衷` |
| 跳过（条件1） | "两个维度" | ≤30字，无问号 |
| 跳过（条件1） | "**同步模型**（synchronous model）" | ≤30字，无问号 |
| 跳过（条件1） | "crash-stop" | ≤30字，无问号 |
| 跳过（条件1） | "安全性全时保证，活性在 GST 后保证" | ≤30字，无问号 |
| 跳过（条件2） | "**FLP 不可能性定理**（FLP impossibility）" | strip 后 = "FLP 不可能性定理"，与核心句实质相同 |
| 跳过（条件2） | "**崩溃故障**（crash fault）" | strip 后 = "崩溃故障"，核心句无法更精炼 |

**插入位置**：文本最前面，加 `---` 分隔：
```
精炼核心句
---
原有摘录内容（一个字符不改）
```
节点的顶层字段 `canvasMargin: { anc: "..." }` 保持不动。

### 步骤 3：批量精炼剩余节点

对剩余待精炼节点批量生成核心句。全部完成后进入下一步。

### 步骤 4：多级树形归类（核心改进）

读完所有节点，构建**多级树形结构**（不是平铺分组）。

#### Step A：读 md 原文，提取结构作为参考

md 原文中若有全景图、ASCII 树、层次列表或任何**结构性骨架**（如第三幕的 "全景图" / "知识图谱" 块），提取出来作为归类**参考**。

- **参考性质**：不是硬约束。md 的结构不一定是树形，可能是表格、对比、流程。不强制对齐。
- **用法**：当 md 结构与 canvas 摘录高度吻合时，骨架可直接套用 md 结构；否则按启发式（Step B）自己归类。
- **无结构性骨架时**：跳过 Step A，直接按 Step B 归类。

#### Step B：启发式层次判定

对每对节点 (A, B)，按以下启发式判定父子关系：

- **归类优先级**：md 结构参考（若有）> 段落嵌套 > 因果词（所以/因此/这意味着）> 保守并列
- **不确定默认并列**，标记 `⚠️待确认`，不猜因果

**因果链正反例**：

| 节点 A | 节点 B | 判定 | 理由 |
|--------|--------|------|------|
| GST 前保安全性 | GST 后保活性 | A→B 因果链 | B 是 A 的对称推论 |
| 崩溃故障 | 拜占庭故障 | 并列 | 同层不同类型 |
| 篡改不是改一个点 | 重做成本 | A→B 因果链 | B 是 A 的后果 |

#### Step C：复用检查（优先复用，禁止新建同义节点）

查找现有节点 N，满足全部三条：
1. N 有顶层字段 `canvasMargin: { anc: "..." }`
2. `strip_markdown(N.text)` 与候选分支名 T 语义等价或包含关系
3. N 尚未被分配为其他分支的子节点

- **找到 N** → `[复用]` N 为分支节点
- **未找到** → `[新建]` 分支标题节点，文本 = `**T**`

**英文别名并入中文父节点**：
若存在一个节点（如 `Vector Clock`）是已有中文节点（如 `向量时钟`）的英文别名，**不作为独立节点重排**：
- 英文名合并到中文父节点标题，格式：
  ```
  **向量时钟**
  ;Vector Clock
  ```
- 原英文节点进入**孤儿 group**（见步骤 5），不真删，保留 canvasMargin 锚点

**复用正反例**（比较前必须先 strip_markdown）：

| 候选分支名 T | 现有节点文本 | strip 后 | 结论 |
|---|---|---|---|
| 通信维度 | `**通信维度**：`（节点有 `canvasMargin` 字段） | `通信维度：` | ✅ 复用 |
| FLP 不可能性定理 | `**FLP 不可能性定理**（FLP impossibility）`（节点有 `canvasMargin` 字段） | `FLP 不可能性定理（FLP impossibility）` | ✅ 复用 |
| 时序保证 | （无匹配节点） | — | ❌ 新建 |

#### Step D：节点在树中的角色与精炼策略

树形确定后，根据节点在树中的角色决定精炼策略：

| 节点角色 | 判定条件 | 精炼策略 |
|----------|----------|----------|
| 叶子节点 | 无子节点 | 精炼句 + `---` + 原文 |
| 非叶节点 | 有子节点 | **只保留结构标签**（≤30字），不加原文，不加精炼句 |

#### Step D.1：非叶节点 = 结构标签（核心约束）

**非叶节点不是摘录内容，是归类结构**。它应该是一个**结构性标签**（如"机制 / 保证 / 局限 / 三条规则 / 困境"），起组织作用，不承载摘录细节。

**参考范式**：md 原文第三幕的"全景图"是非叶节点的标准样本——全部是一两词的结构标签。

**✓ 合格 vs ✗ 不合格 对比**：

```
✓ 合格（非叶节点是结构标签）：
  Lamport 逻辑时钟 C
  ├── 机制              ← 结构标签
  │   └── 三条更新规则
  ├── 保证（单向）       ← 结构标签
  │   └── a→b ⟹ C(a)<C(b)
  └── 局限              ← 结构标签
      └── Lamport 无法区分并发

✗ 不合格（非叶节点是摘录精炼句）：
  Lamport 逻辑时钟 C
  ├── a→b ⟹ C(a)<C(b)   ← 精炼句当了非叶，下面还挂子节点
  │   └── [问句] C(a)<C(b) 能断定 a→b 吗？
  └── 压缩为单数字，并发信息丢失  ← 同样是摘录精炼句被当非叶
```

#### Step D.2：问句节点终极约束（核心约束）

**问句节点（含 `？`）只能做叶子，且父节点必须是陈述节点，禁止与任何骨架节点同级**。

```
✓ 合格（问句是陈述的子节点）：
  陈述 A
  ├── 陈述 A1
  │   └── [问句]        ← 挂在陈述 A1 下面
  └── 陈述 A2

✗ 不合格（问句与陈述并列）：
  陈述 A
  ├── 陈述 A1
  ├── [问句]            ← 与陈述 A1/A2 并列，污染骨架
  └── 陈述 A2

✗ 不合格（问句做非叶）：
  陈述 A
  ├── [问句]            ← 问句永远不能做非叶
  │   └── 子节点
  └── 陈述 A1
```

**正反例**：

| 节点 | 角色 | 处理 |
|------|------|------|
| "机制"（下有三条规则）| 非叶，结构标签 | 只留"机制" |
| "保证（单向）"（下有推论）| 非叶，结构标签 | 只留"保证（单向）" |
| "a→b ⟹ C(a)<C(b)" | 叶子 | 精炼句 + --- + 原文 |
| "[问句] C(a)<C(b) 能断定 a→b 吗？" | 叶子（挂在陈述下）| 原文保留，不精炼 |

#### Step E：输出多级树形草案

输出格式（区分 `[复用]`/`[新建]`/`⚠️待确认`）：
```
拟树形方案：
├── [复用] 通信维度 (id: 70349a97...)
│   ├── 同步模型
│   │   └── Δ太小太大
│   ├── 异步模型
│   │   ├── 沉默与延迟不可区分
│   │   └── 不依赖网络假设
│   └── 半同步模型
│       ├── 半同步假设
│       ├── GST 定义
│       │   └── GST 前后行为
│       ├── GST 前保安全性
│       │   └── GST 后推进 ← ⚠️待确认（因果链 or 并列？）
│       │       └── 安全性全时活性
│       └── GST 永不来
├── [复用] 故障维度 (id: 90ee6172...)
│   └── ...

root：[root 文本 或 无]

⚠️ 标记说明：有 N 处层次关系不确定，已标记。请确认或调整。
确认后写入？
```

如果 `⚠️待确认` 标记 ≥ 3 处，额外生成一个替代方案供用户选择。

用户确认后进入步骤 4.5。

### 步骤 4.5：树形骨架最小样例校准（核心改进）

**对称于步骤 2 的精炼校准**。批量输出完整树形前，先挑**一个分支**（通常是结构最清晰的那支）完整画出三级，让用户确认审美：

```
样例分支（完整三级）：

├── **Lamport 逻辑时钟 C**
│   ├── 机制
│   │   └── 三条更新规则
│   ├── 保证（单向）
│   │   ├── a→b ⟹ C(a)<C(b)
│   │   │   └── [问句] C(a)<C(b) 能断定 a→b 吗？
│   │   └── 单向：C(a)<C(b) ⇏ a→b
│   └── 局限
│       └── Lamport 无法体现并发关系

审美确认点：
1. 非叶节点是否都是结构标签？（机制 / 保证 / 局限）
2. 问句是否都挂在陈述下，不与骨架同级？
3. 层次是否符合你的认知结构？

确认后扩展到全部分支。
```

用户确认后输出完整方案，然后进入步骤 5。若不通过，回到步骤 4 调整。

### 步骤 5/6：生成布局脚本 + 写入 + 验证

步骤 4.5 确认后，**生成并执行以下 Python 脚本**（根据归类结果填入 `LAYOUT`，其余代码结构不变）：

**孤儿节点协议**：
某些节点在归类中被剥离出骨架（如被英文名合并吸收的英文节点、与父节点同义的冗余节点），**不真删**，放入一个 `type:"group"` 框中，标签为 `"孤儿节点（不参与骨架）"`，放在 canvas 左侧（x < 0）。保留所有 `canvasMargin` 锚点不变。

```python
# 孤儿节点示例
ORPHAN_IDS = ["<被合并的英文节点 id>", "<被父节点吸收的冗余 id>"]
# 布局在 x=-800 左右，垂直排列，用 type:"group" 框起
```

**布局参数（预留四列，为后续制卡留空间）**：

```python
import json, copy, uuid

PATH = "<canvas 文件绝对路径>"

# ── 由归类结果填入 ──────────────────────────────────────────
ROOT_ID = "<root 节点 id，无则 None>"

# 多级树形布局：每个节点 (id, children_list, is_reuse, depth)
# depth 决定 x 列：depth=0 → root, depth=1 → 分支, depth=2 → 骨架, depth=3 → 子骨架
# 数据结构为嵌套 tuple：(node_ref, [(child_ref, [...], is_reuse), ...], is_reuse)
TREE = ...  # 由归类结果生成

# Anki 卡片节点 id（不参与重排，edge 也不动）
ANKI_IDS = {"<id>"}
# ── 以上由归类结果填入 ──────────────────────────────────────

# 布局参数（硬编码，预留四列）
COL_X = {
    0: 0,      # root
    1: 380,    # 分支标题
    2: 760,    # 骨架
    3: 1100,   # 子骨架
}
COL_W = {
    0: 220,    # root
    1: 180,    # 分支标题
    2: 260,    # 骨架
    3: 260,    # 子骨架
}
# 第四列 x=1460 预留给 canvas-make-cards 写入卡片
NODE_H     = 60    # 非叶子节点高度
LEAF_H     = 55    # 叶子节点高度
GAP_NODE   = 20    # 同分支内节点间距
GAP_BRANCH = 60    # 分支间距

# ... 递归布局逻辑（按树的深度优先遍历分配 x/y）...

# 写前存档元数据字段
metadata_before = {
    n["id"]: {
        "canvasMargin": copy.deepcopy(n.get("canvasMargin")),
        "canvas2anki": copy.deepcopy(n.get("canvas2anki")),
    }
    for n in canvas["nodes"]
}

# ... 递归分配坐标、生成 edge（全部 right→left）...
# ... 写入 + 验证 ...
```

**布局规则**：
- 所有 edge 统一 `fromSide: "right"`, `toSide: "left"`
- 分支节点 y = 子节点组的垂直中心
- root 节点 y = 所有分支的垂直中心
- 第四列（x=1460, w=380）预留给卡片，本 skill 不写入任何节点到该列

**验证（强制，dry_run 也跑）**：

以下 5 条机械 assert 在写入前必须全部通过，任一失败则不写盘：

```python
errors = []

# Assert 1: 问句必须是陈述的子节点（不与骨架同级，不做非叶）
# 遍历所有节点，找到含 ？ 的 canvasMargin 节点，检查其父节点不含 ？，且自己无子节点
for n in result["nodes"]:
    if n.get("type") != "text": continue
    if "？" in n["text"] and n.get("canvasMargin"):
        parent = find_parent(n["id"], edges, node_map)
        if parent is None:
            errors.append(f"问句节点无父节点: {n['id']}")
        elif "？" in parent["text"]:
            errors.append(f"问句节点父节点也是问句: {n['id']}")
        # 问句节点不能有子节点
        children = find_children(n["id"], edges)
        if children:
            errors.append(f"问句节点不能做非叶: {n['id']}")

# Assert 2: 非叶节点文本 ≤ 30 字（strip markdown 后）
for n in result["nodes"]:
    if n.get("type") != "text": continue
    children = find_children(n["id"], edges)
    if children and n["id"] != ROOT_ID:
        stripped = strip_markdown(n["text"])
        if len(stripped) > 30:
            errors.append(f"非叶节点文本超 30 字: {n['id']} ({len(stripped)}字)")

# Assert 3: 孤儿节点必须在 group 框内
for oid in ORPHAN_IDS:
    o = node_map[oid]
    in_any_group = any(
        g.get("type") == "group" and
        g["x"] <= o["x"] and o["x"] + o["width"] <= g["x"] + g["width"] and
        g["y"] <= o["y"] and o["y"] + o["height"] <= g["y"] + g["height"]
        for g in result["nodes"]
    )
    if not in_any_group:
        errors.append(f"孤儿节点未在 group 框内: {oid}")

# Assert 4: 英文别名节点零遗留（若分支标题含 ;English 格式，原英文节点必须在孤儿中）
for n in result["nodes"]:
    if n.get("type") == "text" and "\n;" in n.get("text", ""):
        # 有英文别名，检查对应英文节点应在 ORPHAN_IDS 中（由归类时记录）
        pass  # 由归类阶段维护 MERGED_EN_IDS，此处验证其都在 ORPHAN_IDS

# Assert 5: 元数据字段原样保留
for n in result["nodes"]:
    nid = n["id"]
    if nid in metadata_before:
        before = metadata_before[nid]
        if before["canvasMargin"] is not None and n.get("canvasMargin") != before["canvasMargin"]:
            errors.append(f"canvasMargin 被修改: {nid}")
        if before["canvas2anki"] is not None and n.get("canvas2anki") != before["canvas2anki"]:
            errors.append(f"canvas2anki 被修改: {nid}")

# 额外检查：无节点 x 落入预留卡片列（x=1460）
for n in result["nodes"]:
    if n.get("x") == 1460 and n["id"] not in ANKI_IDS:
        errors.append(f"节点侵入卡片预留列: {n['id']}")

if errors:
    raise RuntimeError("验证失败:\n" + "\n".join(errors))
```

---

## 硬约束

1. **元数据字段只读**：已有节点的 `canvasMargin` 和 `canvas2anki` 顶层字段不写、不删、不改
2. **文本只插入不修改**：只在最前面加核心句 + `---`，原文一字不改
3. **禁止重复精炼**：核心句与原文实质相同则不插入
4. **用 Python json.dump 写入**：不用 Write 工具直接写 JSON
5. **已精炼节点跳过精炼**：有裸 `---` 的节点不重复处理
6. **Anki 卡片节点不参与重排**：位置不动，其 edge 也不动
7. **树形最小样例校准 + 完整方案都需用户确认**：步骤 4.5 和步骤 4 末尾各一次
8. **优先复用，禁止新建同义节点**：有 `canvasMargin` 字段且语义等价的现有节点直接升为分支，不另建
9. **验证脚本 5 条 assert 不可跳过**：dry_run=true 也跑，失败必须报告
10. **非叶子节点是结构标签，不是摘录精炼句**：有子节点的节点只放 ≤30 字的结构性标签（如"机制/保证/局限"），绝不加原文或精炼内容（Step D.1）
11. **问句节点只做叶子且父节点是陈述**：含 `？` 的节点禁止与任何骨架节点同级，禁止做非叶节点（Step D.2）
12. **问句节点不精炼**：跳过精炼，留作未来 Anki 卡片素材
13. **孤儿节点不真删，放入 group 框**：被剥离骨架的节点（英文别名合并、冗余同义等）进入 `type:"group"` 框，保留 canvasMargin
14. **层次判定保守策略**：不确定时默认并列，标记 `⚠️待确认`，不猜因果
15. **预留卡片列**：x=1460 列不放任何骨架节点，留给 canvas-make-cards

---

## 正反模式（从优质脑图提取）

### 正模式

1. **骨架是推理链，不是平铺列表**
   - ✅ GST 前安全性 → GST 后推进 → 安全性全时活性（因果链）
   - ✅ 篡改不是改一个点 → 重做成本 → 哈希链局限性（推论链）

2. **叶子精炼+原文，非叶节点只放结构标签**
   - ✅ "机制"（非叶，结构标签）
   - ✅ "保证（单向）"（非叶，结构标签）
   - ✅ "沉默与延迟不可区分 --- 原文..."（叶子，精炼+原文）

3. **复用已有 canvasMargin 节点为分支**
   - ✅ "通信维度"摘录节点升级为分支标题，保留跳转锚点

4. **问句挂在陈述下做叶子**
   - ✅ `a→b ⟹ C(a)<C(b)` → `[问句] C(a)<C(b) 能断定 a→b 吗？`
   - 问句永远是陈述的子节点，永远是叶子

5. **冗余节点进入孤儿 group，不真删**
   - ✅ 被英文别名合并的英文节点 → 放入 `type:"group"` 框，保留 canvasMargin

### 反模式

1. **平铺所有子节点到同一层级**
   - ❌ GST 前安全性 / GST 后推进 / 安全性全时 并列在同一层
   - 应该是因果链：A → B → C

2. **把摘录精炼句当非叶节点**（本次踩坑 4 次）
   - ❌ `a→b ⟹ C(a)<C(b)` 下挂问句子节点——精炼句被当成了结构归类标签
   - ✅ 应该新建结构标签"保证（单向）"做非叶，把精炼句和问句都作为它的叶子

3. **问句与骨架节点同级**（本次踩坑 3 次）
   - ❌ 问句和陈述并列在同一父节点下
   - ✅ 问句必须往下挂一层，作为陈述的子节点

4. **新建与已有摘录同义的分支标题**
   - ❌ 已有"通信维度"摘录节点，又新建"**通信模型**"标题节点
   - 应该复用已有节点

5. **删除冗余节点导致 canvasMargin 锚点丢失**
   - ❌ 直接从 canvas 删除英文别名节点
   - ✅ 放入孤儿 group，锚点保留

---

## 输出确认

```
✓ 整理完成
  - 精炼节点：N 个
  - 问句节点（跳过精炼）：N 个
  - 跳过精炼（短节点/实质相同）：N 个
  - 跳过（Anki 卡片）：N 个
  - 复用为分支：N 个
  - 新建分支标题（结构标签）：N 个
  - 英文别名并入中文：N 个
  - 孤儿节点（group 框）：N 个
  - 树形层级：N 级（最大深度）
  - ⚠️ 待确认标记：N 处（已由用户确认）
  - 验证（5 条 assert）：全部通过
  canvas 文件：{路径}
```
